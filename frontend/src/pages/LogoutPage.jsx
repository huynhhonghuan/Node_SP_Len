import React from 'react';
import LogoutButton from '../components/LogoutButton';

const LogoutPage = () => {
    return (
        <div>
            <h1>Logout Page</h1>
            <LogoutButton />
        </div>
    );
};

export default LogoutPage;
